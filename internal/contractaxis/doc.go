// Package contractaxis checks that every named case of an item derived from a
// production universe is bound to a real selector and observable at the outer
// surface, while an axis mutation proves the check can fail.
package contractaxis
