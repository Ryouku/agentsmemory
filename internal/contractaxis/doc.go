// Package contractaxis checks that items derived from a production universe are
// bound to a real selector, observable at the outer surface, and protected by a
// mutation that proves the check can fail.
package contractaxis
